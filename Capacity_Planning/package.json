{
  "name": "zabbix-capacity-planning",
  "version": "1.4.0",
  "private": true,
  "description": "Development and browser-regression tooling for the Zabbix Capacity Planning module.",
  "scripts": {
    "test": "npm run test:build && npm run test:ui",
    "test:build": "node tests/BuildConsistencyTest.cjs",
    "test:ui": "node tests/CapacityPlanningUiTest.cjs",
    "playwright:install": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "1.54.0"
  },
  "engines": {
    "node": ">=20"
  }
}
